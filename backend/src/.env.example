# Environment Configuration
NODE_ENV=development
PORT=3001
HOST=localhost

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# MongoDB Configuration (Document Store)
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_DATABASE=nrn_social
MONGODB_USERNAME=
MONGODB_PASSWORD=
# For MongoDB Atlas (cloud) - use this instead of host/port
MONGODB_URI=

# AWS S3 Configuration (Object Storage for media)
AWS_REGION=af-south-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=nrn-media

# ActivityPub Federation (for later implementation)
FEDERATION_ENABLED=false
FEDERATION_DOMAIN=localhost:3001
FEDERATION_PUBLIC_KEY=
FEDERATION_PRIVATE_KEY=
FEDERATION_USER_AGENT=NRN/1.0.0

# Future databases (commented out for now)
# Redis Cache
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Elasticsearch Search
# ELASTICSEARCH_HOST=localhost
# ELASTICSEARCH_PORT=9200
