# Terraform
*.tfstate
*.tfstate.*
*.tfvars.backup
*.tfplan
.terraform/
.terraform.lock.hcl

# SSH Keys - Keep private key secure but track public key
team-key
# team-key.pub is tracked so teammates can see the public key

# OS
.DS_Store
Thumbs.db
